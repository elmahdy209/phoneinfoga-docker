# 2. nginx.conf
nginx
server {
  listen 80;
  server_name _;

  # Basic Auth setup
  auth_basic "Restricted";
  auth_basic_user_file /etc/nginx/.htpasswd;

  location / {
    proxy_pass http://phoneinfoga:8080;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
  }
}

